///* Added for search on enter on search box. */
$(function () {
    //$.extend($.jgrid.search, {
    //    closeOnEscape: true, searchOnEnter: true, multipleSearch: true, afterRedraw: function ($p) { $('select.opsel').remove(); }
    //});
});



